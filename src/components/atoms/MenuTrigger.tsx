import React from 'react';
import MoreHorizIconStyled from '../atoms/MoreHorizIcon';

const MenuTrigger = ({ onClick }) => {
  return <MoreHorizIconStyled onClick={onClick} />;
};

export default MenuTrigger;
