import styled from '@emotion/styled';

const MenuContainer = styled.div`
  display: flex;
  margin-right: 16px;
  margin-top: 8px;
  cursor: pointer;
  align-items: center;
`;

export default MenuContainer;
